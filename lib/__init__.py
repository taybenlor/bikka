"""
Some useful things
------------------

Templating engine:

    from lib import template
    template.render(html_text, context)
    template.render_file(filenmae, context)

Search engine:

    from lib.search import *

    search(query)
    add_text(post_id, text, post_type)

"""
